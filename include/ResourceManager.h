#ifndef RESOURCEMANAGER_H
#define RESOURCEMANAGER_H

/*
class ResourceManager
{
    public:
        ResourceManager();
        virtual ~ResourceManager();

        Sprite *load_sprite( std::string filename );
        bool unload_sprite( std::string filename );
        bool is_sprite_loaded( std::string filename );
        Sound *load_sound( std::string filename ):
        bool unload_sound( std::string filename );
        bool is_sound_loaded( std::string filename );

        static Sprite ERROR_SPRITE;
        static Sound ERROR_SOUND;
    protected:
    private:
        boost::ptr_map<std::string, Sprite> sprites;
        boost::ptr_map<std::string, Sound> sounds;
};
*/
#endif // RESOURCEMANAGER_H
